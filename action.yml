name: 'move-card-action'
description: 'Move card from project pipeline to devops pipeline'
inputs:
  MY_GITHUB_TOKEN:  # id of input
    required: true
  ISSUE_NUMBER:
    required: true
outputs:
  time: # id of output
    description: 'result'
runs:
  using: 'node12'
  main: 'lib/index.js'